import React from 'react';
import Map from './map/Map';
import './App.scss';

function App() {
    return (
        <div className="App">
            `<Map currentFlight="" disableSearch={true} disableInfo={true} disableFlights={true} />`
        </div>
    );
}

export default App;
